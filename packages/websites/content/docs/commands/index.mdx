---
title: Commands Overview
description: Overview of all SkillUse CLI commands
order: 10
---

SkillUse provides commands for authentication, repository management, skill installation, and multi-agent support.

## Command Categories

### Authentication

| Command | Description |
|---------|-------------|
| `skilluse` | Show status (user, repos, installed skills) |
| `skilluse login` | Authenticate with GitHub |
| `skilluse logout` | Remove stored credentials |

### Repository Management

| Command | Description |
|---------|-------------|
| `skilluse repo list` | List configured repositories |
| `skilluse repo add <repo>` | Add a skill repository |
| `skilluse repo remove <repo>` | Remove a repository |
| `skilluse repo use <repo>` | Set default repository |
| `skilluse repo edit <repo>` | Edit repository settings |

### Skill Management

| Command | Description |
|---------|-------------|
| `skilluse search <keyword>` | Search for skills |
| `skilluse install <skill>` | Install a skill locally |
| `skilluse install <skill> -g` | Install globally |
| `skilluse uninstall <skill>` | Remove an installed skill |
| `skilluse upgrade [skill]` | Upgrade skill(s) to latest |
| `skilluse list` | List installed skills |
| `skilluse list --outdated` | Show skills with updates |
| `skilluse info <skill>` | Show skill details |

### Agent Management

| Command | Description |
|---------|-------------|
| `skilluse agent` | List/select agent interactively |
| `skilluse agent <name>` | Switch to specified agent |

Supported agents: `claude`, `cursor`, `windsurf`, `codex`, `copilot`, `cline`, `roo`, `aider`, `continue`

## Getting Help

Run any command with `--help` for detailed usage:

```bash
skilluse install --help
skilluse repo --help
```

## Detailed Documentation

- [Authentication Commands](/docs/commands/auth)
- [Repository Commands](/docs/commands/repo)
- [Skill Commands](/docs/commands/skill)
